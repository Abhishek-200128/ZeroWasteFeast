Select interpreter in /venv/ZeroWasteFeast_backend/Scripts/python.exe

To install dependencies :
pip install -r requirements.txt

To add new dependencies :
pip freeze > requirements.txt

To start the server:
python -m flask run
